- "temporal_refs": array of objects with "raw" (as it appeared in text), "type" ("point", "range", "duration", or "unresolved"), "resolved" (ISO 8601 date/datetime), optional "resolved_end" (required for ranges), and optional "context". Resolve relative dates using: {{date}}
